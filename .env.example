# Local development only.
# In production, configure via Paperclip plugin settings (instance config + secrets).
# Get a personal API key at https://linear.app/settings/api
LINEAR_API_KEY=

# Optional overrides for `npm run local:sync` (see scripts/local-run.ts):
# LINEAR_TEAM_KEY=ENG
# PAPERCLIP_COMPANY_ID=local-company
# PAPERCLIP_PROJECT_ID=local-project
# PAPERCLIP_AGENT_ID=local-agent
# LINEAR_ISSUE_FILTER={"state":{"name":{"eq":"Todo"}}}
